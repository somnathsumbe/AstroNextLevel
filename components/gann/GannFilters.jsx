function optionLabel(value, icons) {
  const icon = icons[value];
  return icon ? `${icon} ${value}` : value;
}

export default function GannFilters({ values, options, icons, onChange, onReset }) {
  return <section className="filter-panel gann-today-filters" aria-label="Today stock filters"><div className="filter-title"><span><i className="bi bi-funnel" /> Filter Pressure Dates</span><span className="filter-count">All filters combine</span></div><div className="row g-3 align-items-end"><div className="col-12 col-md-6 col-xl-3"><label htmlFor="today-stock-filter">Stock Filter</label><select id="today-stock-filter" className="form-select" value={values.stock} onChange={(event) => onChange('stock', event.target.value)}><option value="all">All Stocks</option>{options.stocks.map((stock) => <option key={stock} value={stock}>{stock}</option>)}</select></div><div className="col-12 col-md-6 col-xl-3"><label htmlFor="today-planet-filter">Planet Filter</label><select id="today-planet-filter" className="form-select" value={values.planet} onChange={(event) => onChange('planet', event.target.value)}><option value="all">All Planets</option>{options.planets.map((planet) => <option key={planet} value={planet}>{optionLabel(planet, icons)}</option>)}</select></div><div className="col-12 col-md-6 col-xl-3"><label htmlFor="today-pressure-filter">Today Angle Stock</label><select id="today-pressure-filter" className="form-select" value={values.today} onChange={(event) => onChange('today', event.target.value)}><option value="all">All Pressure Dates</option><option value="today">Today's Pressure Date</option></select></div><div className="col-12 col-md-6 col-xl-3"><label htmlFor="today-priority-filter">Priority</label><select id="today-priority-filter" className="form-select" value={values.priority} onChange={(event) => onChange('priority', event.target.value)}><option value="all">All</option>{options.priorities.map((priority) => <option key={priority} value={priority}>{priority}</option>)}</select></div><div className="col-12 col-lg-8"><label htmlFor="today-stock-search">Search Stock</label><div className="filter-search"><i className="bi bi-search" /><input id="today-stock-search" className="form-control" value={values.search} onChange={(event) => onChange('search', event.target.value)} placeholder="Search stock name..." /></div></div><div className="col-12 col-lg-4 filter-actions gann-reset-action"><button className="subtle-action" type="button" onClick={onReset}><i className="bi bi-arrow-counterclockwise" /> Reset Filters</button></div></div></section>;
}