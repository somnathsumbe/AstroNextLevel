export const metadata = {
  title: 'Register',
  description: 'Create a new Astro Market Analytics account.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function RegisterLayout({ children }) {
  return children;
}