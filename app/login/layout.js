export const metadata = {
  title: 'Login',
  description: 'Sign in to the Astro Market Analytics workspace.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function LoginLayout({ children }) {
  return children;
}